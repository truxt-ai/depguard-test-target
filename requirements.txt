# Intentionally outdated Python dependencies for DepGuard testing
# Contains known CVEs, outdated versions, and co-dependency chains

# Flask stack - co-dependent chain (flask requires jinja2 + werkzeug)
flask==0.12.0
jinja2==2.9.0
werkzeug==0.14.0

# HTTP stack
requests==2.18.0
urllib3==1.22.0
certifi==2018.1.18

# Task queue stack - co-dependent (celery requires kombu)
celery==4.1.0
kombu==4.1.0

# Web framework with known CVEs
django==2.0.0

# Crypto with known vulnerabilities
cryptography==2.1.0

# Image processing
pillow==5.0.0

# ORM
sqlalchemy==1.2.0

# Serialization - CVE exists
pyyaml==6.0.3

# XML parsing - CVE exists
lxml==3.7.0
