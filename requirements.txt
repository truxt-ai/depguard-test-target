# Flask stack — co-dependent chain (tests CONDITIONAL co-upgrade grouping)
flask==0.12.0
jinja2==2.9.0
werkzeug==0.14.0

# HTTP stack
requests==2.18.0
urllib3==1.22.0
certifi==2018.1.18

# Task queue stack
celery==4.1.0
kombu==4.1.0

# High-profile CVEs — CRITICAL severity
django==2.0.0
cryptography==2.1.0
pillow==5.0.0
sqlalchemy==1.2.0
pyyaml==3.12
lxml==3.7.0

# XML / parsing with known CVEs
pyotp==2.2.0
paramiko==2.4.0

# Data science with CVEs
numpy==1.14.0
scipy==1.0.0

# Dev dependencies (LOW CVE → auto-dismissed by auto_dismiss_dev_low_severity)
# These are in requirements-dev.txt conceptually but listed here for scanner coverage
